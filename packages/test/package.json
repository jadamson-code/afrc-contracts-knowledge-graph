{
  "name": "@sigma/test",
  "private": true,
  "dependencies": {
    "@playwright/test": "^1.49.1",
    "playwright": "^1.49.1",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "test": "npm run test:type && npm run test:e2e",
    "test:type": "vitest typecheck --run type && tsc --noEmit -p tsconfig.typecheck.json && tsc --noEmit -p tsconfig.typecheck-without-satellites.json",
    "test:e2e": "playwright test"
  }
}
